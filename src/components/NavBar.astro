---
const links = [
  { name: "About", href: "/" },
  { name: "Projects", href: "/projects" },
  { name: "Experience", href: "/experience" },
  { name: "Leadership", href: "/leadership" },
];
---

<nav class="fixed top-4 z-50">
  <div class="flex justify-end">
    <div class="hidden md:flex gap-4 text-poppins text-white border px-4 py-1 rounded-2xl backdrop-blur-md">
      {links.map(link => (
        <a href={link.href} class="hover:text-gray-300">{link.name}</a>
      ))}
    </div>
  </div>
</nav>
